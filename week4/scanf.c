#include <stdio.h>
#include <stdlib.h>

int main(void)
{
    // Using scanf with integers
    // int x;

    // printf("x: ");
    // scanf("%i", &x);
    // printf("x: %i\n", x);

    // Using scanf with strings
    char *s;

    printf("s: ");
    scanf("%s", s);
    printf("s: %s\n", s);
}
